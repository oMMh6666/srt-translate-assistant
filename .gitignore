# ============================================================
# 目录命名约定（看目录名就知道它是什么）
#
#   不带点  = 跑项目用得着（进版本库）
#             core/ app/ web/ prompts/ log/ input/ api_keys/
#
#   . 前缀  = 与运行无关，本地自用的东西，随时可删
#             .tmp/  .docs/  .workbuddy/  （.venv/ .git/ 属工具链）
#
# 例外：.tests/ **带点但照样进版本库** —— 它是正式资产不是草稿。
#   冒烟脚本以前放 Tmp/（被忽略），被当草稿清掉过一次，回归验证能力整个没了。
#   所以「目录加不加 .」和「进不进仓库」是两件事，别混为一谈。
# ============================================================

# --- 一、运行时数据目录（参与运行，目录进仓库 / 内容不进）---

# 真实 API Key：绝对不能进仓库。
#   原方案是「提交空壳 + git update-index --skip-worktree 锁定」，
#   但 skip-worktree 是本地索引状态，clone/重建索引时会失效、有泄密风险。
#   这里直接忽略，安全优先。
api_keys/*.db
api_keys/*.db-wal
api_keys/*.db-shm

# 任务库（每个任务一个 db）+ 浏览器上传的字幕暂存
log/*
!log/.gitkeep
input/*
!input/.gitkeep

# 提示词：reflect.md 要；custom_prompt.md（0 字节）要；其余 custom_prompt.*.md 不要
prompts/custom_prompt.*.md
!prompts/custom_prompt.md

# --- 二、与运行无关的本地目录（. 前缀，一律不进仓库）---
.tmp/
.docs/
.workbuddy/

# --- 三、旧代码 / 本地自用文件 ---
legacy/
push.bat
History.md

# --- 四、工具链与编辑器 ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.vscode/
*.log
