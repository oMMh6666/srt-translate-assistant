# ============================================================
# 注意：api_keys/api_keys.db 里是真实的 API Key，绝对不能进仓库。
#   原方案是「提交空壳 + git update-index --skip-worktree 锁定」，
#   但 skip-worktree 是本地索引状态，clone/重建索引时会失效、有泄密风险。
#   这里直接忽略，安全优先。
# ============================================================
api_keys/*.db
api_keys/*.db-wal
api_keys/*.db-shm

# --- 旧命令行代码 ---
legacy/

# --- 日志目录：目录进仓库，内容不进 ---
log/*
!log/.gitkeep

# --- 提示词：reflect.md 要；custom_prompt.md（0 字节）要；其余 custom_prompt.*.md 不要 ---
prompts/custom_prompt.*.md
!prompts/custom_prompt.md

# --- 临时 / 无关目录 ---
docs/
input/
tests/
Tmp/

# --- 记录文件 ---
History.md

# --- 本地推送脚本（自用，不进仓库） ---
push.bat

# --- Python 环境与编辑器 ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.vscode/
*.log

# --- 会话记忆（本地状态，不进仓库） ---
.workbuddy/
